import { createTheme } from '@mui/material/styles';

export const darkTheme = createTheme({
  palette: {
    mode: 'dark',
    action: {
      disabledBackground: '#b52218',
      disabled: '#ffffff',
    },
  },
});
